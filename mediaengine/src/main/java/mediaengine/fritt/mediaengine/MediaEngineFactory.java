package mediaengine.fritt.mediaengine;

import org.webrtc.PeerConnectionFactory;

public class MediaEngineFactory extends PeerConnectionFactory {
    public MediaEngineFactory(Options options) {
        super(options);
    }
}
